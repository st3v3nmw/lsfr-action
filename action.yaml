name: Test
description: Test coding challenges with lc

inputs:
  working-directory:
    description: Directory containing lc.state
    required: false
    default: '.'
  lc-version:
    description: Version of lc to install
    required: false
    default: 'v0.2.0'

runs:
  using: composite
  steps:
    - name: Setup Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.25'

    - name: Cache lc
      uses: actions/cache@v5
      id: cache-lc
      with:
        path: ~/go/bin/lc
        key: lc-${{ runner.os }}-${{ runner.arch }}-${{ inputs.lc-version }}

    - name: Install lc
      if: steps.cache-lc.outputs.cache-hit != 'true'
      shell: bash
      run: go install github.com/littleclusters/lc/cmd/lc@${{ inputs.lc-version }}

    - name: Run tests
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: ${{ github.action_path }}/test.sh
